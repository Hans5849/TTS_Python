# Service generation

Speech_Common generates `tts-python.service` from the installation manifest and converts old service names to aliases of that same unit. Do not copy a historical unit template: it cannot know custom paths, service account, protected data, or credential availability.
